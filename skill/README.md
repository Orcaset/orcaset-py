# Install `orcaset-py` skill

Install the `orcaset-py` skill by pasting the command below into your coding agent.

```txt
Install the skill from https://github.com/Orcaset/orcaset-py/tree/main/skill/orcaset-py
```

Alternatively, download this repo and copy the skill files manually into your agent's skills directory.
