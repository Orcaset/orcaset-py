[project]
name = "paper-lbo"
version = "0.1.0"
description = "Simple leveraged buyout model with IRR sensitivity"
requires-python = ">=3.14"
dependencies = [
    "numpy-financial>=1.0.0",
    "orcaset==0.9.0",
]

[tool.uv.sources]
orcaset = { path = "../../", editable = true }
