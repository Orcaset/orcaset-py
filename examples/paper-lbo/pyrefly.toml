# Run from this directory (`uv run pyrefly check`) so local modules and
# example-only dependencies resolve independently of the library.
project-excludes = [".venv", "**/__pycache__", "references"]
search-path = ["."]
strict-callable-subtyping = true

[errors]
implicit-any = true
