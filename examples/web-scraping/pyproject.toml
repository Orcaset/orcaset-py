[project]
name = "web-scraping"
version = "0.1.0"
description = "Nowcast Southwest passenger revenue from TSA checkpoint volumes"
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4>=4.13.0",
    "httpx>=0.28.0",
    "orcaset==0.7.1",
    "types-beautifulsoup4>=4.12.0",
]

[tool.uv.sources]
orcaset = { path = "../../", editable = true }
